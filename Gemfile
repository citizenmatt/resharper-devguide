source "https://rubygems.org"

# First time restore with "bundle install --path _vendor/bundle"
# Subsequent restore with just "bundle"

gem 'jekyll'

group :jekyll_plugins do
  gem 'jekyll-redirect-from'
end
